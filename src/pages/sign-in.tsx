import { Button } from "@/components/ui/button";

export default function SignIn() {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <Button>Sign In with EPIC</Button>
    </div>
  );
}
